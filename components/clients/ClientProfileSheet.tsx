/**
 * Client profile surface (tabs: Profile, Notes, Membership, Service History).
 * Alias for the component rendered at `/clients/[id]`.
 */
export { ClientDetailView as ClientProfileSheet } from "./ClientDetailView";
